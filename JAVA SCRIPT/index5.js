//  Ques-- For the given string:
// let msg = "   hello  ";
// trim it and convert it to uppercase using method chaining.

let msg = "   hello  ";
let newMsg = msg.trim().toUpperCase();
console.log(newMsg);

// Ques--For the String Let name = "ApnaCollage", predict the output for following :
 let name = "ApnaCollage";
  let newName = name.replace("Apna","Collage");
    console.log(newName);