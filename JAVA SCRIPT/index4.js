let msg = "   hello  ";
console.log(msg)
// let newMsg = msg.trim();
// console.log(newMsg);
// newMsg = newMsg.toUpperCase();
// console.log(newMsg);


// METHOD CHANNING...!
let newMsg = msg.trim().toUpperCase();
console.log(newMsg);

//  REPLACE METHOD.!
//  Search for a specified value in a string, and returns a new string where the specified values are replaced.
let str = "i love coding ";
 let newStr = str.replace("coding", "programming");
console.log(newStr);

//  REPEAT METHOD..!
// Returns a new string with a specified number of copies of an existing string.
let str1 = "hello ";
let newStr1 = str1.repeat(3);
console.log(newStr1);